# -------------------------------
# MapProxy example configuration.
# -------------------------------
#
# This is a minimal MapProxy configuration.
# See full_example.yaml and the documentation for more options.
#

# Starts the following services:
# Demo:
#     http://localhost:8080/demo
# WMS:
#     capabilities: http://localhost:8080/service?REQUEST=GetCapabilities
# WMTS:
#     capabilities: http://localhost:8080/wmts/1.0.0/WMTSCapabilities.xml
#     first tile: http://localhost:8080/wmts/osm/webmercator/0/0/0.png
# Tile service (compatible with OSM/etc.)
#     first tile: http://localhost:8080/tiles/osm/webmercator/0/0/0.png
# TMS:
#     note: TMS is not compatible with OSM/Google Maps/etc.
#     fist tile: http://localhost:8080/tms/1.0.0/osm/webmercator/0/0/0.png
# KML:
#     initial doc: http://localhost:8080/kml/osm/webmercator

#base: ['base/globals.yaml','base/sourcesgd.yaml','base/servicewmsgd.yaml']
base: ['base/globals.yaml','base/grids.yaml']

services:
  demo:
  wms:
    # only offer WMS 1.1.1
    versions: ['1.3.0', '1.1.1']

    # supported SRS for this WMS
    srs: ['EPSG:2056']

    # force the layer extents (BBOX) to be displayed in this SRS
    bbox_srs: ['EPSG:2056']

    # limit the supported image formats.
    image_formats: ['image/jpeg', 'image/png', 'image/gif', 'image/GeoTIFF', 'image/tiff']

    # add attribution text in the lower-right corner.
    attribution:
      text: '(c) GIS Daten AG'

    # return an OGC service exception when one or more sources return errors
    # or no response at all (e.g. timeout)
    on_source_errors: raise

    # some WMS clients do not send all required parameters in feature info
    # requests, MapProxy ignores these errors unless you set strict to true.
    strict: false

    # list of feature info types the server should offer
    featureinfo_types: ['json', 'html', 'text', 'xml']

    md:
      # metadata used in capabilities documents
      title: Eignungskarte fuer Erdwaermesondenbohrungen (NW und OW)
      abstract: WMS Dienste zu Erdwaermesondierungen fuer NW und OW
      online_resource: https://www.gis-daten.ch
      contact:
        organization: GIS Daten AG
        address: Aemaettlistrasse 2
        city: Stans
        postcode: 6370
        country: Switzerland
        phone: +41 (0)41 618 61 12
        email: support@gis-daten.ch
      # multiline strings are possible with the right indention
      access_constraints:
        Dieser WMS darf nur fuer das Bundesamt fuer Energie verwendet werden.
  # wmts:
    # # only offer WMS 1.1.1
    # versions: ['1.3.0', '1.1.1']

    # # supported SRS for this WMS
    # srs: ['EPSG:2056']

    # # force the layer extents (BBOX) to be displayed in this SRS
    # bbox_srs: ['EPSG:2056']

    # # limit the supported image formats.
    # image_formats: ['image/jpeg', 'image/png', 'image/gif', 'image/GeoTIFF', 'image/tiff']

    # # add attribution text in the lower-right corner.
    # attribution:
      # text: 'Rasterdaten (c) swisstopo, AV-Daten (c) GIS Daten AG'

    # # return an OGC service exception when one or more sources return errors
    # # or no response at all (e.g. timeout)
    # on_source_errors: raise

    # # some WMS clients do not send all required parameters in feature info
    # # requests, MapProxy ignores these errors unless you set strict to true.
    # strict: false

    # # list of feature info types the server should offer
    # featureinfo_types: ['text', 'xml']

    # md:
      # # metadata used in capabilities documents
      # title: WMTS Basisdaten GIS Daten AG
      # abstract: WMTS Dienste fuer Amt fur kbs NW
      # online_resource: https://www.gis-daten.ch
      # contact:
        # organization: GIS Daten AG
        # address: Aemaettlistrasse 2
        # city: Stans
        # postcode: 6370
        # country: Switzerland
        # phone: +41 (0)41 618 61 12
        # email: support@gis-daten.ch
      # # multiline strings are possible with the right indention
      # access_constraints:
        # Dieser WMTS darf nur fuer das Amt fuer Lanwirtschaft Nidwalden verwendet werden.

parts:
    coverages:
        bboxnw: &bboxnw
            bbox: [2643483, 1171738, 2702269, 1216819]
            srs: 'EPSG:2056'
        bboxow: &bboxow
            bbox: [2643483, 1171738, 2702269, 1216819]
            srs: 'EPSG:2056'
        bboxnwow: &bboxnwow
            bbox: [2643483, 1171738, 2702269, 1216819]
            srs: 'EPSG:2056'
        bboxlu: &bboxlu
            bbox: [2630127, 1180568, 2681764, 1237690]
            srs: 'EPSG:2056'
        bboxels: &bboxels
            bbox: [2620000, 1146000, 2724800, 1242000]
            srs: 'EPSG:2056'
        bboxclip: &bboxclip
            bbox: [2660000, 1172000, 2680000, 1200000]
            #datasource: Point(2660000, 1172000).buffer(2000)
            srs: 'EPSG:2056'

layers:
  - name: ch.nw.waermenutzung.legende
    title: Legende Waermenutzung NW
    legendurl: https://www.gis-daten.ch/docs/mapproxy_legends/nw_waermenutzung.png
    
  - name: ch.ow.waermenutzung.legende
    title: Legende Waermenutzung OW
    legendurl: https://www.gis-daten.ch/docs/mapproxy_legends/ow_waermenutzung.png
       
  - name: ch.nw.waermenutzungsbereiche
    title: Waermenutzungsbereiche NW
    sources: [ch.nw.wnb]
    
  - name: ch.nw.grundwasserwaermepumpen_entnahme
    title: Grundwasserwaermepumpen Entnahme NW
    sources: [ch.nw.gwwp_ent]    

  - name: ch.nw.grundwasserwaermepumpen_rueckgabe
    title: Grundwasserwaermepumpen Rueckgabe NW
    sources: [ch.nw.gwwp_ruck]    

  - name: ch.nw.oberflaechenwaermepumpen_entnahme
    title: Oberflaechenwaermepumpen Entnahme NW
    sources: [ch.nw.ofwp_ent]    

  - name: ch.nw.oberflaechenwaermepumpen_rueckgabe
    title: Oberflaechenwaermepumpen Rueckgabe NW
    sources: [ch.nw.ofwp_ruck]    

  - name: ch.nw.waermepumpe_mit_erdsonden
    title: Waermepumpe mit Erdsonden NW
    sources: [ch.nw.wp_erdson]    
  
  - name: ch.nw.waermepumpe_mit_erdregistern
    title: Waermepumpe mit Erdregistern NW
    sources: [ch.nw.wp_erdreg]
       
  - name: ch.ow.ews_zulaessigkeit
    title: EWS Zulaessigkeit (OW)
    sources: [ch.ow.ews_zulaessigkeit]
  
sources:       
# Nidwalden
      ch.nw.wnb:
        type: arcgis
        http:
           ssl_no_cert_checks: True
           # defines how long MapProxy should wait for data from source servers
           client_timeout: 600 # seconds
           # add additional HTTP headers to all requests to your sources.
           headers:
               X-Esri-Authorization: Bearer qv5_yXLe29elI80KU48VI5LSqPPm9j3DsAHLBaYkSVN8B5oQkK0MD-jUYlZ52K_P
        req:
          layers: show:7
          url: https://gisdaten-ags-01.gisdaten.local:6443/arcgis/rest/services/nw_afu/nw_waermenutzung/MapServer
          transparent: true
        opts:
          featureinfo: true
          featureinfo_tolerance: 10
          featureinfo_return_geometries: false
        image:
          transparent_color: '#ffffff'
          # opacity: 0.8
        supported_srs: ['EPSG:2056']
        coverage: *bboxnw
        
      ch.nw.gwwp_ent:
        type: arcgis
        http:
           ssl_no_cert_checks: True
           # defines how long MapProxy should wait for data from source servers
           client_timeout: 600 # seconds
           # add additional HTTP headers to all requests to your sources.
           headers:
               X-Esri-Authorization: Bearer qv5_yXLe29elI80KU48VI5LSqPPm9j3DsAHLBaYkSVN8B5oQkK0MD-jUYlZ52K_P
        req:
          layers: show:1
          url: https://gisdaten-ags-01.gisdaten.local:6443/arcgis/rest/services/nw_afu/nw_waermenutzung/MapServer
          transparent: true
        opts:
          featureinfo: true
          featureinfo_tolerance: 10
          featureinfo_return_geometries: false
        image:
          transparent_color: '#ffffff'
          # opacity: 0.8
        supported_srs: ['EPSG:2056']
        coverage: *bboxnw
        
      ch.nw.gwwp_ruck:
        type: arcgis
        http:
           ssl_no_cert_checks: True
           # defines how long MapProxy should wait for data from source servers
           client_timeout: 600 # seconds
           # add additional HTTP headers to all requests to your sources.
           headers:
               X-Esri-Authorization: Bearer qv5_yXLe29elI80KU48VI5LSqPPm9j3DsAHLBaYkSVN8B5oQkK0MD-jUYlZ52K_P
        req:
          layers: show:2
          url: https://gisdaten-ags-01.gisdaten.local:6443/arcgis/rest/services/nw_afu/nw_waermenutzung/MapServer
          transparent: true
        opts:
          featureinfo: true
          featureinfo_tolerance: 10
          featureinfo_return_geometries: false
        image:
          transparent_color: '#ffffff'
          # opacity: 0.8
        supported_srs: ['EPSG:2056']
        coverage: *bboxnw
        
      ch.nw.ofwp_ent:
        type: arcgis
        http:
           ssl_no_cert_checks: True
           # defines how long MapProxy should wait for data from source servers
           client_timeout: 600 # seconds
           # add additional HTTP headers to all requests to your sources.
           headers:
               X-Esri-Authorization: Bearer qv5_yXLe29elI80KU48VI5LSqPPm9j3DsAHLBaYkSVN8B5oQkK0MD-jUYlZ52K_P
        req:
          layers: show:3
          url: https://gisdaten-ags-01.gisdaten.local:6443/arcgis/rest/services/nw_afu/nw_waermenutzung/MapServer
          transparent: true
        opts:
          featureinfo: true
          featureinfo_tolerance: 10
          featureinfo_return_geometries: false
        image:
          transparent_color: '#ffffff'
          # opacity: 0.8
        supported_srs: ['EPSG:2056']
        coverage: *bboxnw
        
      ch.nw.ofwp_ruck:
        type: arcgis
        http:
           ssl_no_cert_checks: True
           # defines how long MapProxy should wait for data from source servers
           client_timeout: 600 # seconds
           # add additional HTTP headers to all requests to your sources.
           headers:
               X-Esri-Authorization: Bearer qv5_yXLe29elI80KU48VI5LSqPPm9j3DsAHLBaYkSVN8B5oQkK0MD-jUYlZ52K_P
        req:
          layers: show:4
          url: https://gisdaten-ags-01.gisdaten.local:6443/arcgis/rest/services/nw_afu/nw_waermenutzung/MapServer
          transparent: true
        opts:
          featureinfo: true
          featureinfo_tolerance: 10
          featureinfo_return_geometries: false
        image:
          transparent_color: '#ffffff'
          # opacity: 0.8
        supported_srs: ['EPSG:2056']
        coverage: *bboxnw
        
        
      ch.nw.wp_erdson:
        type: arcgis
        http:
           ssl_no_cert_checks: True
           # defines how long MapProxy should wait for data from source servers
           client_timeout: 600 # seconds
           # add additional HTTP headers to all requests to your sources.
           headers:
               X-Esri-Authorization: Bearer qv5_yXLe29elI80KU48VI5LSqPPm9j3DsAHLBaYkSVN8B5oQkK0MD-jUYlZ52K_P
        req:
          layers: show:5
          url: https://gisdaten-ags-01.gisdaten.local:6443/arcgis/rest/services/nw_afu/nw_waermenutzung/MapServer
          transparent: true
        opts:
          featureinfo: true
          featureinfo_tolerance: 10
          featureinfo_return_geometries: false
        image:
          transparent_color: '#ffffff'
          # opacity: 0.8
        supported_srs: ['EPSG:2056']
        coverage: *bboxnw
        
      ch.nw.wp_erdreg:
        type: arcgis
        http:
           ssl_no_cert_checks: True
           # defines how long MapProxy should wait for data from source servers
           client_timeout: 600 # seconds
           # add additional HTTP headers to all requests to your sources.
           headers:
               X-Esri-Authorization: Bearer qv5_yXLe29elI80KU48VI5LSqPPm9j3DsAHLBaYkSVN8B5oQkK0MD-jUYlZ52K_P
        req:
          layers: show:6
          url: https://gisdaten-ags-01.gisdaten.local:6443/arcgis/rest/services/nw_afu/nw_waermenutzung/MapServer
          transparent: true
        opts:
          featureinfo: true
          featureinfo_tolerance: 10
          featureinfo_return_geometries: false
        image:
          transparent_color: '#ffffff'
          # opacity: 0.8
        supported_srs: ['EPSG:2056']
        coverage: *bboxnw

# Obwalden
      ch.ow.ews_zulaessigkeit:
        type: arcgis
        req:
          layers: show:6
          url: https://gisdaten-ags-01.gisdaten.local:6443/arcgis/rest/services/gis_fach/ow_waermenutzung/MapServer
          transparent: true
        opts:
          featureinfo: true
          featureinfo_tolerance: 10
          featureinfo_return_geometries: false
        image:
          transparent_color: '#ffffff'
          # opacity: 0.8
        supported_srs: ['EPSG:2056']
        #grid: epsg_2056
        coverage: *bboxow
   
  
globals:
  srs:
    axis_order_en: ['EPSG:21781', 'EPSG:2056']
    # override system projection file
    #proj_data_dir: '/path to dir that contains epsg file'

  # cache options
  cache:
    # where to store the cached images
    base_dir: './cache_data'
    # where to store lockfiles for concurrent_requests
    lock_dir: './cache_data/locks'
    # where to store lockfiles for tile creation
    tile_lock_dir: './cache_data/tile_locks'

    # request x*y tiles in one step
    meta_size: [4, 4]
    # add a buffer on all sides (in pixel) when requesting
    # new images
    meta_buffer: 80


  # image/transformation options
  image:
    # use best resampling for vector data
    resampling_method: bicubic # nearest/bilinear
    # stretch cached images by this factor before
    # using the next level
    stretch_factor: 1.15
    # shrink cached images up to this factor before
    # returning an empty image (for the first level)
    max_shrink_factor: 4.0

    # Enable 24bit PNG images. Defaults to true (8bit PNG)
    paletted: false
    formats:
      custom_format:
        format: image/png
        # the custom format will be stored as 8bit PNG
        mode: P
        colors: 32
        transparent: true
        encoding_options:
        # The algorithm used to quantize (reduce) the image colors
          quantizer: fastoctree
      # edit an existing format
      image/jpeg:
        encoding_options:
          # jpeg quality [0-100]
          jpeg_quality: 80
        
        
        
        
        
        
        
        
        